import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  images: { unoptimized: true },
  basePath: "/Farida-Emad-Portfolio",
  assetPrefix: "/Farida-Emad-Portfolio",
};

export default nextConfig;
